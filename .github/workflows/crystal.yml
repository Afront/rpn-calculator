name: Crystal CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    container:
      image: crystallang/crystal

    steps:
    - uses: actions/checkout@v2
    - name: Test the RPN Calculator
      run: |
        cd rpn-calculator 
        shards install 
        crystal spec
